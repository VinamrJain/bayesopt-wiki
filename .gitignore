# OS / editor
.DS_Store
.idea/
.vscode/

# Python / secrets
__pycache__/
.venv/
.env
.env.*

# PDFs (parsed md + LaTeX are tracked; binaries stay local)
*.pdf

# Superseded drafts
_archive/

# Wiki tooling
wiki/.obsidian/

# Claude Code local agent config (settings, hooks, permissions). Kept local, not
# committed. Git-ignoring does NOT affect Claude — it reads these from disk by path.
.claude/

# raw/: track only LaTeX source, bib, and good parsed md
raw/**
!raw/**/
!raw/**/*.tex
!raw/**/*.bib
!raw/**/*.md
