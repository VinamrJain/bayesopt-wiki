---
note: notation
last_reviewed: 2026-06-06
verdict: open-gaps
open_gaps: 6
reviewer: claude-opus-4-8 (reflection-sweep reviewer subagent)
scope: sweep
---

## 2026-06-06 — sweep (inaugural full-wiki reflection)
sources read: wiki/notation.md, wiki/expected-improvement.md, wiki/gp-ucb.md, wiki/bo-as-dynamic-program.md, wiki/cost-aware-stopping.md, wiki/pandoras-box-gittins-index.md (lambda usage), wiki/map.md (regret-gp-bandits registry), references.md (citation keys)

- [notation-1] gap (open) — Symbol collision on $\lambda$. notation.md (line 45) defines $\lambda$ canonically as the GP NOISE VARIANCE, and it is used that way in bo-as-dynamic-program.md (transition variance $\sigma_n^2+\lambda$) and gp-ucb.md's crosswalk. But cost-aware-stopping.md (delta line 33) and pandoras-box-gittins-index.md (delta line 29) reuse $\lambda$ as the COST-SCALING / objective-to-cost conversion factor. Both cost notes declare it in their Notation delta but neither flags that it OVERRIDES the canonical noise-variance meaning, so a reader inheriting notation.md hits a silent clash on a load-bearing symbol. The canonical table is the right place to resolve this (the table is the single symbol source). Resolve the $\lambda$ overload from notation.md: either (a) add a row in the cost-aware section noting that $\lambda$ is reused as the cost-scaling factor in the cost-per-sample/PBGI notes (analogous to how $\alpha_n$ vs $\alpha_k$ is explicitly disambiguated), or (b) pick a distinct symbol for noise variance (e.g. $\sigma_\varepsilon^2$). If (a), the two cost notes' deltas should say they override the canonical $\lambda$.
- [notation-2] gap (open) — Index-convention mismatch on the UCB exploration weight. The notation.md table (line 76) lists the symbol as $\beta_t$, but the wiki's stated convention is n-indexing (line 48 defines $E_n$, and gp-ucb.md explicitly states 'we index by $n$ evaluations' and uses $\beta_n$ as canonical throughout, crosswalking $\beta_t$ as srinivas2010's round-indexed form). The canonical table thus leaks the source's $t$-indexing for the one symbol where the wiki took care to convert. Mismatch between the symbol table ($\beta_t$) and its primary consumer ($\beta_n$). Change the table entry to $\beta_n$ (UCB exploration weight), matching the wiki's n-indexing convention and gp-ucb.md's canonical usage; leave $\beta_t$ to the per-note srinivas2010 crosswalk.
- [notation-3] nit (accepted-fixed) — Dangling wikilink [[regret-gp-bandits]] (lines 113, plus referenced from gp-ucb, max-value-entropy-search, gaussian-process-regression, acquisition-functions). The target wiki/regret-gp-bandits.md does not exist; it appears in map.md only as a planned/forward-reference node. This is a wiki-wide unresolved link rather than a notation-specific bug, and the build_index linter currently tolerates it, but notation.md is one of the carriers. Either create the regret-gp-bandits stub (concept-grade) so the 5 inbound links resolve, or remove the forward reference from notation.md's $\gamma_T$ row until the note exists. Track wiki-wide rather than fixing notation.md in isolation.
- [promote-1] gap (open) — [symbol-promotion] Promote S^n (state of knowledge) and V^n / V^* / pi (DP value functions & policy) (used in bo-as-dynamic-program, budget-constrained-dp, nonmyopic-cost-constrained-bo, multistep-budgeted-bo) into notation.md. Promote the DP state symbol S^n (state of knowledge / posterior after n evals), the value functions V^n and V^*, and the policy pi into notation.md as a 'dynamic programming / sequential decision' block. They recur as per-note deltas in all 4 DP-family notes (bo-as-dynamic-program defines S^n/V^n/pi; budget-constrained-dp adds V^*/V^n/pi; nonmyopic-cost-constrained-bo uses V^pi_h/C^pi_h/pi; multistep-budgeted-bo uses Q_n). notation.md currently has NO state-of-knowledge or value-function entry (grep confirmed).
- [promote-2] gap (open) — [symbol-promotion] Promote u(D_n) terminal utility and s(D_n) total cost (used in budget-constrained-dp, bo-as-dynamic-program, multistep-budgeted-bo, cost-aware-stopping) into notation.md. Promote terminal utility u(D_n)=max y (=f^*_n noise-free) and total cost s(D_n) into notation.md. terminal utility appears in 4+ notes (bo-as-dynamic-program 10x, budget-constrained-dp 3x, multistep-budgeted-bo 1x) and total cost / s(D_n) in budget-constrained-dp; centralizing u and s removes the repeated 'equals the incumbent f^*_n' reconciliation and fixes the f^*_n/u(D_n)/f^*_t drift noted under terminology.
- [promote-3] gap (open) — [symbol-promotion] Promote Q_n state-action value (n-step) (used in multistep-budgeted-bo, bo-as-dynamic-program, budget-constrained-dp) into notation.md. Consider promoting Q_n(x|D) (n-step state-action value) alongside V^n into the DP notation block, since the Bellman recursions in multistep-budgeted-bo, bo-as-dynamic-program, and budget-constrained-dp all rely on a Q/V pair. Lower priority than S^n/V^n but belongs in the same block for consistency.
